# TODO Demo

Простий REST API на Node.js + Express + MongoDB.

## 📦 Установка
npm install

## 🛠 Запуск
npm start

## 🚀 Ендпоінти
GET /api/todos — список
POST /api/todos — створити
PUT /api/todos/:id — оновити
DELETE /api/todos/:id — видалити
